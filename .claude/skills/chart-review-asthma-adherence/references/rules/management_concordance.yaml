# T2 — Management concordance. Each rule asks: did the management plan
# document the EPR-3 / 2020 Focused Update recommended action, and if
# not, is the deviation attributable?

rules:
  - rule_id: R-T2-StepTherapyMatches
    description: >-
      The patient's controller regimen matches guideline step therapy for
      their control level, using the stepwise table for their AGE BAND
      (T0-AgeBand): EPR-3 Figures 4-1a/4-2a (ages 2-4), 4-1b/4-2b (ages 5-11),
      and Figure 4-5 + 2020 SMART/LAMA (ages 12-17).
    naepp_source: >-
      EPR-3 Section 4 age-band stepwise tables: Figures 4-1a/4-2a (ages 0-4),
      Figures 4-1b/4-2b (ages 5-11), Figure 4-5 (Youths ≥12 and Adults,
      p.343). For ages 12+: 2020 Focused Update Rec 12 (strong) and Rec 13
      (conditional) p.80-86 — ICS-formoterol SMART for moderate-severe
      persistent asthma; Recs 14-16 p.90-95 — LAMA add-on as Step 4
      alternative when LABA is contraindicated or unavailable. Per Rec 12,
      ICS-formoterol SMART applies to ages >=4 (available for the 5-11 band); LAMA
      stays ages >=12. For ages 2-4, use the pediatric figures (SMART only for 4-year-olds).
    evidence_grade: A_with_SMART_strong_LAMA_conditional
    event_anchor: asthma_encounters
    # Per-event: judge only visits where a control picture is ascertainable.
    # undetermined = "no control picture ascertainable" too, so it is
    # excluded from evaluability the same as an absent answer.
    event_evaluable_if: "T1-ControlLevel is present and T1-ControlLevel != \"undetermined\""
    verdict_if: "T2-StepTherapyMatch == \"matches\""
    excluded_if: "T2-StepTherapyMatch == \"unknown\""
      # v0.6: collapsed to the design document's five categories.
      # PATIENT_REFUSAL and CONTRAINDICATION were refinements of PATIENT_FACTOR,
      # which the design defines as "documented clinical reason for deviation
      # (allergy, contraindication, patient preference)" — all three arms of this
      # question land there. They were the question's ENUM VALUES promoted to
      # top-level categories, which is how a five-category taxonomy became nine.
    attribution_when:
      - when: "T2-ContraindicationDocumented in [\"contraindication\", \"patient_refusal\", \"pending_followup\"]"
        category: PATIENT_FACTOR
      - when: "T2-ContraindicationDocumented == \"system_barrier\""
        category: SYSTEM_FACTOR
      - when: "T2-ContraindicationDocumented == \"not_documented\""
        category: DOCUMENTATION_GAP
    attribution: GUIDELINE_DEVIATION
    nuanced: true
    judge_can_override: false
    # T1-ControlLevel added 2026-08-28: `event_evaluable_if` READS it — it is what
    # decides whether this rule applies at a visit at all — and it was the one
    # input the reviewer's pane never listed. The other direction (declared but
    # unread) is fine and stays: those inform whoever answers, and the pane now
    # shows them under "Context (not read by the engine)".
    supporting_questions:
      [T0-AgeBand, T1-ControlLevel, T2-StepTherapyMatch, T2-ContraindicationDocumented,
       T1-ControllerPrescribed]

  - rule_id: R-T2-WrittenActionPlan
    description: >-
      Patient has a written asthma action plan documented in the
      lookback window.
    naepp_source: >-
      EPR-3 Component 2 p.94 (Key Points: Patient Education) — 'Provide
      all patients with a written asthma action plan that includes (1)
      daily management and (2) how to recognize and handle worsening
      asthma. Written action plans are particularly recommended for
      patients who have moderate or severe persistent asthma, a history
      of severe exacerbations, or poorly controlled asthma
      (Evidence B).' Reinforced at p.97 — 'Clinicians provide all
      patients with a written asthma action plan' (Evidence B).
    evidence_grade: B
    verdict_if: "T2-WrittenActionPlan == true"
    attribution: DOCUMENTATION_GAP
    supporting_questions: [T2-WrittenActionPlan]

  - rule_id: R-T2-FollowupScheduled
    description: >-
      Follow-up scheduled within 3 months for non-controlled patients.
      Well-controlled patients (T1-ControlLevel == well_controlled) are
      excluded — they can be reviewed at routine 6-month intervals.
    naepp_source: >-
      EPR-3 Component 2 p.94 (Key Points) — 'Regular review, by an
      informed clinician, of the status of the patient's asthma
      control is an essential part of asthma self-management education
      (Evidence B).' EPR-3 Section 4 stepwise tables recommend follow-up
      at 2-6 weeks when initiating/stepping up, then every 1-6 months
      when controlled. 3 months is the cap for non-controlled patients.
    evidence_grade: B
    event_anchor: [asthma_encounters, ocs_bursts]
    # "within 3 months of this event" — the SPAN this rule is judged over, not a
    # display hint. Until 2026-08-27 nothing read it: the 90 days lived only in
    # the question's prose ("within 3 months"), leaving the agent to hold the
    # boundary in its head, while the question's own EVENT SCOPE paragraph told
    # it to answer "as of the event date" — a point, for a span question. Same
    # defect the controller obligation's deadline had, and the same fix: the
    # span is stated once, both bounds named, and the engine knows it.
    event_window_days: 90
    # A burst always needs follow-up; a visit only when not well controlled
    # at THAT visit.
    event_evaluable_if: "_anchor_type == \"ocs_bursts\" or (T1-ControlLevel is present and T1-ControlLevel != \"well_controlled\")"
    # AN UNOBSERVED TAIL MAKES ONLY A NEGATIVE INCONCLUSIVE. When the 90 days run
    # past the end of observation, a follow-up that IS documented still settles
    # the event — the requirement is met and no further observation can unmeet it.
    # A follow-up that is NOT documented settles nothing: it may have been
    # arranged in the days the extract does not cover. So this censors the
    # negative only, rather than dropping the event, which would throw away
    # conclusive positives. `_window_censored` is the engine comparing
    # event_window_days above against the ETL's `days_to_index`.
    event_censored_if: '_window_censored == true and T2-FollowupScheduled != true'
    event_censored_reason: >-
      the 3-month judgment window runs past the end of observation and no
      follow-up was documented within the part that was observed — the chart
      cannot show whether one was arranged in the remainder
    verdict_if: "T2-FollowupScheduled == true"
    attribution: DOCUMENTATION_GAP
    supporting_questions: [T2-FollowupScheduled, T1-ControlLevel]

  - rule_id: R-T2-InhalerTechniqueChecked
    description: >-
      Inhaler technique was assessed (and corrected if needed) at
      least once in the lookback window.
    naepp_source: >-
      EPR-3 Component 2 p.94 (Key Points) — 'Skills: e.g., inhaler
      technique, use of a valved holding chamber (VHC) or spacer, and
      self-monitoring' are required teaching items. EPR-3 Component 4
      p.220 (Safety of ICS, Key Points) — 'Evaluate patient adherence
      and inhaler technique [...] before increasing the dose of ICS
      (Evidence B).' EPR-3 p.243 — 'Spacers or valved holding
      chambers used with non-breath-activated MDIs reduce local side
      effects (Evidence A).'
    evidence_grade: B
    verdict_if: "T2-InhalerTechniqueChecked in [\"assessed_correct\", \"assessed_corrected\"]"
    attribution: DOCUMENTATION_GAP
    nuanced: true
    judge_can_override: false
    supporting_questions: [T2-InhalerTechniqueChecked]

  - rule_id: R-T2-ComorbidityAddressed
    description: >-
      For patients with documented asthma-relevant comorbidities, the
      chart shows a management plan (referral, treatment, lifestyle
      counseling). Identifying a comorbidity without addressing it is
      itself a gap.
    naepp_source: >-
      EPR-3 Component 3 p.166 — 'Treating the conditions may improve
      asthma management' (per-comorbidity evidence grades A-D). EPR-3
      p.165 — allergen avoidance multifaceted (Evidence A); tobacco
      smoke avoidance (Evidence C); influenza vaccination (Evidence A).
    evidence_grade: B
    verdict_if: "T2-ComorbidityAddressed == \"addressed\""
    excluded_if: "T2-ComorbidityAddressed == \"not_applicable\""
    attribution: DOCUMENTATION_GAP
    nuanced: true
    judge_can_override: false
    supporting_questions: [T1-ComorbidityAssessed, T2-ComorbidityAddressed]

  - rule_id: R-T2-SpecialtyReferralWhenIndicated
    description: >-
      Not-well-controlled (T1-WorstControlLevel — the worst level at any visit
      in the period — in not_well_controlled / very_poorly_controlled) or
      Step 4+ patients have a documented specialty referral OR are already
      receiving specialty care (an actual pulmonology / allergy-immunology
      visit in the encounters data counts). Well-controlled, sub-Step-4
      patients are excluded (referral not indicated). Unlike the comorbidity
      rule, the exclusion stays on the ANSWER here rather than moving into
      excluded_if: the Step-4+ arm of the indication has no question backing
      it (nothing records the patient's step), so the engine cannot decide
      applicability on its own.
    naepp_source: >-
      EPR-3 Section 4 p.373 (Referral to an Asthma Specialist) — referral is
      recommended for a life-threatening exacerbation, failure to meet therapy
      goals after 3-6 months, atypical signs/symptoms, need for additional
      testing, or when the patient requires Step 4 care or higher (Step 3 for
      children 0-4). Design domain E ('referral to specialist if indicated').
    evidence_grade: D
    # `not_indicated` is caught by excluded_if first, so listing it in
    # verdict_if was a dead arm — dropped.
    verdict_if: "T2-SpecialtyReferral == \"referred\""
    excluded_if: "T2-SpecialtyReferral == \"not_indicated\""
    attribution: DOCUMENTATION_GAP
    nuanced: true
    judge_can_override: false
    # T2-StepTherapyMatch is deliberately NOT listed: it is answered per event,
    # so it has no patient-level value this period rule could read, and listing
    # it made the reviewer's "Inputs:" row show a permanently blank input. The
    # step level that feeds the Step-4+ arm reaches the rule through the
    # annotator's `not_indicated` choice, not as an answer.
    supporting_questions: [T1-WorstControlLevel, T2-SpecialtyReferral]
