{
  "name": "@chart-review/agent-provider",
  "version": "0.1.0",
  "private": true,
  "description": "Agent runtime orchestrator: AgentProvider interface, provider factory (lazy-import-based), runAgent(input). Default provider selection via AGENT_PROVIDER env, per-call override via input.provider.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@chart-review/agent-compose": "*"
  }
}
