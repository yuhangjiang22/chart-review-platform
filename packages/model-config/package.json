{
  "name": "@chart-review/model-config",
  "version": "0.1.0",
  "private": true,
  "description": "Per-feature Claude model selection (modelFor + describeAllModels). The seam for swapping models without grepping env reads.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}
