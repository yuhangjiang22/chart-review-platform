{
  "name": "@chart-review/rule-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic rule DSL for adherence tasks. Evaluates boolean expressions over QuestionAnswer[] and emits RuleVerdict[]. LLM-as-judge path is invoked only when rule.nuanced === true (caller passes a judge function).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@chart-review/platform-types": "*"
  }
}
